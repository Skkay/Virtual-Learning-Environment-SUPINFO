{% extends 'base.html.twig' %}

{% block title %}{{ 'admin.level.title.show'|trans }}{% endblock %}

{% block body %}
<ul>
    <li><b>{{ 'admin.level.misc.id'|trans }}:</b> {{ level.id }}</li>
    <li><b>{{ 'admin.level.misc.label'|trans }}:</b> {{ level.label }}</li>
    <li><b>{{ 'admin.level.misc.students'|trans }}:</b>
        <ul>
        {% if level.students is empty %}
            <li>-</li>
        {% else %}
            {% for student in level.students %}
                <li>{{ student.user.email }}</li>
            {% endfor %}
        {% endif %}
        </ul>
    </li>
</ul>
{% endblock %}
