version: "3.4"

services:
  php:
    build:
      context: .
      target: symfony_php
      args:
        SYMFONY_VERSION: ${SYMFONY_VERSION:-}
        SKELETON: ${SKELETON:-symfony/skeleton}
        STABILITY: ${STABILITY:-stable}
    restart: unless-stopped
    depends_on:
      - database
    volumes:
      - php_socket:/var/run/php
    healthcheck:
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s
    environment:
      # Run "composer require symfony/orm-pack" to install and configure Doctrine ORM
      DATABASE_URL: mysql://${DATABASE_USER:-symfony}:${DATABASE_PASSWORD:-ChangeMe}@database:3306/${DATABASE_NAME:-app}?serverVersion=mariadb-${DATABASE_VERSION:-10.7.4}

  caddy:
    build:
      context: .
      target: symfony_caddy
    depends_on:
      - php
    environment:
      SERVER_NAME: ${SERVER_NAME:-localhost, caddy:80}
    restart: unless-stopped
    volumes:
      - php_socket:/var/run/php
      - caddy_data:/data
      - caddy_config:/config
    ports:
      # HTTP
      - target: 80
        published: 80
        protocol: tcp
      # HTTPS
      - target: 443
        published: 443
        protocol: tcp
      # HTTP/3
      - target: 443
        published: 443
        protocol: udp

  database:
    image: mariadb:${DATABASE_VERSION:-10.7.4}
    restart: always
    volumes:
      - mariadb_data:/var/lib/mariadb/data:rw
    environment:
      - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD:-ChangeMe}
      - MARIADB_DATABASE=${DATABASE_NAME:-app}
      - MARIADB_USER=${DATABASE_USER:-symfony}
      - MARIADB_PASSWORD=${DATABASE_PASSWORD:-ChangeMe}

volumes:
  php_socket:
  caddy_data:
  caddy_config:
  mariadb_data:
